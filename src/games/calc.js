import readlineSync, { getRandomNumber, welcome } from '../index.js';

const calcOfExpressions = () => {
  const greeting = welcome();
  console.log('What is the result of the expression?');
};
export default calcOfExpressions;
